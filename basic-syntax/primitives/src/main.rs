fn main() {
    // signed int: i8, i16, i32, i64, i128, isize
    let _size:isize = 10; 
    
    // unsigned int: u8, u16, u32, u64, u128, usize
    let _num: u8 = 10;

    // floating point: f32, f64
    let _dec: f32 = 10.213;

    // Array & Tuple
    let _arr = [1, 2, 3, 4]; 
    let _tup = (1, true);

}
